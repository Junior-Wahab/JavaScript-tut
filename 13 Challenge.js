let x;
x = Math.floor(Math.random() * 100 + 1);
let y = 51;
y = Math.floor(Math.random() * 50 + 1);

let sum;
sum = x + y;
sum = `${x} + ${y} = ${sum}`;

let subtract;
subtract = x - y;
subtract = `${x} - ${y} = ${subtract}`;

let multiple;
multiple = x * y;
multiple = `${x} + ${y} = ${multiple}`;

console.log(sum);
console.log(subtract);
console.log(multiple);
